# chess
rob's chess game

this will use flass chess and other crap i guess.

venv:
python -m venv venv
venv\Scripts\activate

- run venv, install libraries, then freeze requirements:
pip freeze > requirements.txt
pip install -r requirements.txt